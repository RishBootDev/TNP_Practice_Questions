Tree Notes
============================================================

1. the tree is non linear data structure
2. the tree consists nodes that connected by the edges

IQ difference between the linear and non linear data structure?

3. The time complexity of the linear data gets increased with the increased size in the data
4. terminology:
      Node : It is an entity that contains data and their references to the another node
      Edge : it is a link between two nodes
      Root : topmost node of the tree (it is the endpoint of the tree)
      Height : the height of the tree is the height of the root node bottom to top
      Depth : arrow from top to bottom

5. Binary Tree : is a tree data structure where each parent node can have atmost 2 child nodes
       the node consist of three parts

       class TreeNode {
             int data;
             TreeNode left;
             TreeNode right;
       }

6. there are three traversal techniques : preorder , postorder and inorder

      Inorder : left -> root -> right
      PostOrder : root -> left -> right
      PreOrder : left -> right -> root

7. programs on traversal
8. Types of BT :
      1. Full BT
      program : check the bt is full or not??

9. A complete binary tree is a special type of binary tree where all levels are completely filled,
   except possibly for the last level, which is filled from left to right. It acts like a "packed"
   structure with no gaps between nodes on the lowest level.

10. A perfect binary tree is a specific type of binary tree in which every internal node has exactly
   two children and all leaf nodes are at the same depth (level). It is a highly symmetric and balanced
    structure where every level is completely filled.
